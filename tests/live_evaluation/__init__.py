"""Live-evaluation test package.

Namespacing prevents generic module names such as ``test_config`` and
``test_runner`` from colliding with the legacy acceptance suite during a full
pytest collection.
"""
