# Live evaluation discrepancy register

Campaign: `evaluation-studio-v1`

This register is the authoritative list of unresolved or superseded evidence
conditions for the full product-surface campaign. A discrepancy remains open
until a later append-only checkpoint contains direct, reconciled evidence. No
historical ledger row or evidence root is rewritten to make a discrepancy
disappear.

| ID | State | Scope | Observation | Acceptance impact | Reconciliation rule |
| --- | --- | --- | --- | --- | --- |
| D-001 | Open | Production economics | The persistent production reservation ledger contains `$0.00001312` actual spend and `$0.00000028` retained ambiguous exposure, while the historical Regulus execution-event count is zero. | Blocks `economics.reconciled-totals` and `stop.no-economic-double-count`. | Make one fresh tagged provider call through the corrected pipeline; require exactly one reservation, signed audit cost identity, and Regulus execution event with totals within `max($0.000001, 0.5%)`. Never backfill the historical gap. |
| D-002 | Controlled | Synthetic economics | A `$0.01` synthetic control reservation exists. It is not deployment usage. | It must not appear as production or per-deployment spend. | Present `synthetic_control_usd` separately. Combined all-evidence spend is diagnostic only. |
| D-003 | Open | Provider credential | The credential previously pasted into chat or exposed in diagnostics is excluded from the campaign. | Blocks live provider verification, retrieval/tool calls, Workflow 1/2 paid paths, and measured Rightsizing. | Install a newly rotated credential through Zeroth's external secret provider; rerun only the bounded tagged matrices. Never store it in the repository or evidence. |
| D-004 | Reconciled | Native Safari | Earlier Computer Use attempts were blocked by the macOS lock screen, and older framebuffer captures omitted or blanked the content pane. On 2026-08-25, native Safari rendered the current Studio UI, restored parent graph v3 with child version 2 after reload, submitted provider-free loop run `7e67746d5e4641c08eb9f699a4ddd57a`, and restored the same visible Succeeded run identity after a second real reload. A later Retention inspection reproduced the blank content pane with a complete accessibility tree; seven legacy page roots still owned the transform-based `z-fade`. Removing that page-root animation made Retention visibly paint while preserving its connected tenant/role/policy/hold state through refresh. | Native Safari current-version inspection, loop refresh, and Retention paint evidence are no longer blocked. Provider-backed Workflow 2 refresh acceptance remains separately blocked by D-003. | `native-safari-loop-refresh-checkpoint-20260825-1` contains the exact run/runtime/audit/economics proof. `native-safari-retention-validation-checkpoint-20260825-1` contains three painted Safari screenshots, paired accessibility snapshots, exact runtime scope/policy/hold state, and checksums. `workflow2-parent-v3-pin-checkpoint-20260824-1` remains the immutable parent/child deployment proof. |
| D-005 | Superseded | Chroma checkpoint | `chroma-corpus-checkpoint-20260824-1` stopped before sealing because lint failed. | No acceptance claim derives from `-1`. | `chroma-corpus-checkpoint-20260824-2` is sealed and proves the three persisted synthetic documents; the live embedding probe remains separate. |
| D-006 | Open | Chroma live probe | The tenant-qualified corpus is persisted and verified, but the required newly instrumented embedding probe has not run with a rotated credential. | Blocks `control.chroma-probe-reconciled` and live semantic retrieval acceptance. | Run one tagged embedding probe and correlate provider request, signed audit, reservation, execution event, and corpus IDs. |
| D-007 | Open | Shared-provider cross-check | A clean provider-project usage window is unavailable while the live credential is excluded. | Blocks `economics.shared-provider-window-upper-bound`. | Capture a bounded shared-project window around the fresh tagged calls and treat it only as an upper-bound check. |
| D-008 | Diagnostic | Immutable evidence attempts | Unsealed or explicitly superseded roots remain on disk, including the first economics, security-rejection, Chroma, and Workflow 3 capture attempts. | They must not be used as passing sources. | Keep them append-only for forensic context; source-map entries may reference only accepted sealed roots. |
| D-009 | Reconciled | Handoff checkpoint | `handoff-checkpoint-20260824-1` recorded Docker's full JSON label set, including an unnecessary local secret-mount pathname but no credential value. Subsequent append-only handoffs superseded it as accepted evidence expanded. | The overly broad command record remains excluded from acceptance. | `handoff-checkpoint-20260825-4` projects only service, state, health, and ports and binds the current Safari loop, Retention, all-route control inventory, economics/Chroma, and gap-audit roots. Source maps may reference only the current handoff root. |
| D-010 | Reconciled | Structured-token batching | The original default-token implementation serialized branch dispatch and the closed diagnostic snapshot measured peaks `[1, 1, 1]`. The structured source claim now delegates one bounded wave to the branch-isolated parallel executor and settles fan-in once, preserving ordered output, child isolation, timeout/cancellation fencing, approval continuation, and canonical parent checkpointing. The accepted live rerun produced peaks `[4, 4, 4]` across three UI-driven eight-item repetitions. A later cold-backend diagnostic exposed a separate SQLite startup race: every transaction re-issued the database-wide WAL transition. WAL initialization is now serialized once per database instance; the first eight-item run after restart completed in about three seconds instead of timing out. | Default-token happy-path concurrency, post-restart lineage, and batching recovery are accepted. Paid provider economics remains separately blocked by D-003. | `provider-free-batch-concurrency-20260826-3` is the authoritative sealed source: runs `e7fa1a4c9eef431f8761ee5395041204`, `e779cd4d6b67440c8cb7c21c9ecd865d`, and `737dd03ff24b4db1a293be7423eef302` contain 24 isolated children, ordered branches 0 through 7, verified signed parent chains, zero provider cost, and measured peaks `[4, 4, 4]`. The original peak-one snapshot remains diagnostic only. Cold-start WAL serialization is guarded by `tests/test_async_database.py::TestAsyncSQLiteDatabase::test_concurrent_first_transactions_initialize_wal_once`; the isolated post-fix service run `ce77249523534e2f9790ac6249745cce` is supplemental staging evidence and is not a replacement for the sealed source. |
| D-011 | Open | Provider credential containment | A backend-environment diagnostic on 2026-08-25 printed the configured OpenAI credential into transient tool output. The two provider declarations were immediately removed from `.dev-secrets/zeroth.env`; both backend containers were force-recreated, and a name-only inspection confirms that neither recreated container carries an OpenAI or campaign-provider credential. The incident also exposed that the dev launcher hard-required a provider key for provider-free workflows. A failing-first regression now permits sanitized provider-free startup while retaining fail-closed secret resolution when a workflow requests a provider. | The exposed credential is compromised and excluded. Live-provider acceptance remains stopped until upstream revocation and installation of a newly rotated credential. Provider-free deterministic work may continue on the sanitized services. | Revoke the exposed credential at OpenAI, supply a new credential through the external secret provider, verify it without logging or persisting it, and rerun only the bounded tagged live-provider gates. The sealed containment evidence is `provider-credential-containment-20260825-1`; the regression is `tests/scripts/test_zeroth_dev_instance.py::test_backend_environment_allows_provider_free_start_without_provider_key`. Both primary and twin health checks passed after sanitized recreation. |
| D-012 | Reconciled | Child-subgraph approvals | The product path discovers child-owned approvals through bounded, scope-checked ancestry; atomically commits one signed child-to-parent continuation notification; reconciles a resolution after restart; and resumes only the pinned child while preserving sibling delivery. The persistent campaign deployment was upgraded and exercised through the real Approvals UI. | The provider-independent child-pause, refresh, partial-collection, signed-chain, exactly-once, and zero-cost gate is closed. Provider-backed Workflow 2 economics remain separately blocked by D-003. | `workflow2-child-pause-accepted-20260826-1` is the authoritative sealed evidence: two parent runs, sixteen child runs, approve/reject paths, no sibling replay, eight screenshots, video, signed chains, and zero provider/cost identities. Exact D-012 remains the restored serving target. |
| D-013 | Reconciled | Webhook transition/audit atomicity | Create, deactivate, enqueue/fan-out, replay enqueue, delivered, failed, and dead-letter mutations compose with canonical signed-audit and chain-head writes in one shared service-database transaction. The provider-independent SQLite matrix proves the complete 16-case transition/fault surface. Three exact PostgreSQL 17 cases additionally inject failure after the audit insert for create, deactivate, and dead-letter replay, then prove domain state, audit row, and chain head roll back together. | The former generic `transactional_state_and_audit_outbox` and PostgreSQL-parity blockers are closed for a single service database. This is not evidence for PostgreSQL storage loss or multi-region failover. | `webhook-atomicity-d013-20260826-2` remains the accepted SQLite checkpoint. `webhook-postgres-atomicity-d013-20260826-1` is the additive PostgreSQL checkpoint: all three exact cases pass, every checksum verifies, recursive secret scanning passes, provider/external-network counts are zero, and the live D-012 service was not mutated. The prior SQLite `-1` root remains a sealed failed attempt after it caught a test-helper `signer` binding regression. |
| D-014 | Partially reconciled | Shared SQLite process resilience | A real spawned-process matrix now proves Zeroth's production async SQLite wrapper and local action sink on one native single-host filesystem: concurrent writers converge on 32 unique rows under 128 attempts, lock timeout fails closed, retry after release commits, process exit before commit rolls back, restart commits cleanly, and four duplicate action deliveries yield one durable marker with conflict refusal. Both databases retain WAL mode and pass full integrity checks. The earlier live corruption and disk I/O failure occurred when two Docker Desktop containers shared a host bind-mounted SQLite file, which is a different locking/cache domain. | Native single-host multi-process behavior is accepted. The persistent two-container bind-mounted topology and cross-host SQLite remain blocked; this checkpoint must not be cited to restart the twin writer against the current host-mounted database. | `sqlite-native-multiprocess-accepted-20260826-3` is the authoritative sealed checkpoint with command exit code, sanitized matrix, closed database snapshots, recursive secret scan, and checksums. The `-1` root remains immutable historical evidence because its pre-fix snapshot scan created WAL/SHM sidecars; `-2` was the clean post-fix rehearsal before final source verification. Immutable read-only scanning now has a failing-first regression. Move shared writers onto one verified native Docker volume or PostgreSQL, then run the same matrix in that exact deployment domain before closing the topology blocker. |
| D-015 | Reconciled | Legacy control-gate evidence | The original checksum-valid control bundle embeds the operational `webhook_subscriptions.secret` database column and therefore fails the current recursive secret policy. A broader matcher also initially mistook `authorization_log_id` audit correlation metadata for a credential field. | The raw legacy bundle is excluded from accepted-source auditing; its 14 mapped criteria previously appeared as evidence failures. Secret detection must not be weakened to admit the raw database. | `control-gate-sanitized-accepted-20260826-2` verifies the original SHA-256 manifest and SQLite integrity externally, retains only hashes/sizes/table counts plus secret-clean events, and proves the budget-cap commands independently. Authorization correlation IDs are allowed, while authorization headers and credential-bearing columns remain forbidden. After the separate deterministic provider-fault checkpoint was added and structured Workflow 1 output was explicitly classified as credential-blocked, the conservative source-map audit is 112 pass / 0 fail / 21 blocked / 8 not-run in `interim-acceptance-gap-audit-20260826-5`. |

## Current accepted accounting split

- Production actual spend: `$0.00001312`
- Production ambiguous exposure: `$0.00000028`
- Synthetic control: `$0.01`
- Combined all-evidence diagnostic total: `$0.01001312`

The discrepancy register is not a waiver. Any unexplained secret exposure,
cross-tenant record, broken signed chain, cost mismatch, or unintended external
side effect remains an immediate campaign stop condition.
