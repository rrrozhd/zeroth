"""Vendored from governai 0.2.3 (see zeroth/core/governed/PROVENANCE.md).

The governed tool primitives, merged into the maintained agent runtime by
the backend architecture refactor (Task 14). ``ExecutionPlacement`` is
contract-owned vocabulary re-imported from
:mod:`zeroth.contracts.registry.tooling`.
"""
